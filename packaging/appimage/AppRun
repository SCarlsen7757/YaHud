#!/bin/sh
# Launch YaHud from inside the AppImage with a stable working directory so
# ASP.NET resolves its content root next to the binary.
HERE="$(dirname "$(readlink -f "$0")")"
cd "$HERE/usr/bin" || exit 1
exec ./YaHud "$@"
